# Self-loop state — chess-engine-trainer

## Baselines (2026-09-03, after rounds 1–5)

- Tests: `bun test.js` → 19 passed, 0 failed
- Bench: depth-4 startpos 2843 nodes / best b1c3 score 10; mate-in-1 (Qxf7) 188 nodes
- Depth-6 probe: 19966 nodes / ~0.03s / ~650–700k nps, best b1c3 score 10
- Self-play probe: ~2650–2740 games/min single-thread (depth 1)
- Worker pool (train.js --threads): depth-1 sustained ~3000/min (1 thread) → ~7000/min (4 threads); depth-2 iters ~1380/min → ~2320/min (4 threads steady-state)
- Depth-6 probe command:
  `bun -e "const{makeEngine}=require('./src/engine.js');const E=makeEngine();const th=E.defaultTheta();const t0=Date.now();const r=E.think(E.startPos(),th,{depth:6});const dt=(Date.now()-t0)/1000;console.log('depth6:',r.nodes,'nodes,',dt.toFixed(2)+'s,',((r.nodes/dt)/1000).toFixed(1)+'k nps, best',E.moveToString(E.startPos(),r.move))"`

## Backlog (top first)
1. `feat`: real piece mobility in eval (mob term currently counts knights only)
2. `feat`: king-zone attack weights in eval (beyond pawn shield)
3. `perf`: time-managed think (deadline-aware iterative deepening already; add node cap + early exit on stable best)
4. `fix`: audit `texelTune` SPSA step for divergence on small datasets (probe loss variance)
5. `chore`: opening variety — wider book for self-play to reduce drawish repetition


## Run log

- R1 `fix`: proper SPSA scaling (was 18x too big, thrashed) + aspiration centered on move score (depth-4 nodes 17876→16843) + dead-code removal. 19/19.
- R2 `perf`: incremental Zobrist (`pos.h` in do/undo/null), hash consistency 30/30 games. 19/19, identical node counts.
- R3 `perf`: reverse + depth-1 futility with `legal>0` mate guard. 19/19, depth-6 nodes 154908→150714.
- R4 perf: eval cache keyed on pos.h (Map, scoped per think; bypassed outside search). 19/19. depth-4 nodes identical 16843, mate 268; depth-6 150714->150867 nodes (+0.1pct, 2x 32-bit key collisions), same best b1c3 score 10; depth-6 113ms->97ms (~+15pct nps); self-play ~540->~610 games/min.
- R5 perf: root PVS (full window first move, null-window + re-search rest; replaces per-move aspiration). 19/19. depth-4 16843->2843 nodes, mate 268->188, depth-6 150867->19966 nodes same best b1c3 score 10, self-play ~610->~2700 games/min.
- R6 perf: worker-thread pool for self-play (src/pool.js + play-worker.js, spsaIterParallel, train.js --threads; self-scheduling dispatch, null-pool fallback). 19/19. depth-4/depth-6/mate node counts identical (2843/19966/188); depth-1 sustained ~3000->~7000 games/min on 4 threads, depth-2 iters ~1380->~2320/min steady-state
